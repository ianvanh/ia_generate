<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DrkBot | Image Generator</title>
  <meta name="description" content="Generator De Imágenes Gratuito">
  <meta property="og:image" content="https://raw.githubusercontent.com/ianvanh/ia_generate/master/public/logo.png">

  <link rel="apple-touch-icon" sizes="180x180" href="https://raw.githubusercontent.com/ianvanh/ia_generate/master/public/logo.png" />
  <link rel="icon" type="image/png" sizes="32x32" href="https://raw.githubusercontent.com/ianvanh/ia_generate/master/public/logo.png" />
  
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <style type="text/css" media="all">
    * {
      text-decoration: none !important;
    }
    header {
      padding: 20px 9%;
      background: linear-gradient(
        to bottom right, 
        hsl(240, 1%, 25%) 3%, 
        hsl(0, 0%, 19%) 97%);
      box-shadow: 0 16px 30px hsla(0, 0%, 0%, 0.25);
    }
    .logo {
      font-family: 'Roboto Mono', monospace;
      font-size: 30px;
      font-weight: 700;
      color: white;
    }
    textarea {
      width: 65%;
      height: 70px;
      justify-content: center;
      display: flex;
      padding: 13px 20px;
      margin: 40px auto;
      outline: none;
      border-radius: 5px;
      background: black;
      color: hsl(0,3.1%,95.2%);
      font-size: 15px;
      border: 1px solid hsl(353.6, 92.2%, 50%);
      box-shadow: 5px -5px 20px rgba(255, 255, 255, 0.1),
                  5px 5px 10px rgba(0, 0, 0, 1), inset -2px -2px 5px rgba(255, 255, 255, 0.1),
                  inset 2px 2px 5px rgba(0, 0, 0, 0.5), 0 0 0 2px #1f1f1f;
    }
    textarea:hover {
      box-shadow: 0 0 35px hsl(353.6, 92.2%, 50%);
      background: black;
      border: 1px solid hsl(353.6, 92.2%, 50%);
    }
    textarea::placeholder {
      color: hsl(0, 0%, 100%);
      opacity: .7;
      font-size: 15px;
    }
    myBtn {
      width: 60%;
      height: 40px;
      margin: 10px auto;
      justify-content: center;
      display: block;
      color: #fff;
      background: var(--black);
      font-size: 1em;
      font-weight: bold;
      margin-top: 20px;
      outline: none;
      border-radius: 5px;
      transition: 0.2s ease-in;
      cursor: pointer;
    }
    .imgGenerate {
      width: 100%;
      height: auto;
    }
  </style>
</head>
<body style="background: #00a2d0;">
  <header class="text-center">
    <a href="/" class="logo">DrkBot</a>
  </header>

  <div class="container pt-5">
    <section class="text-center">
      <div class="text-center">
          <h1 class="text-black">Generador De Imagenes</h1>
      </div>
      
      <form action="/generate" method="POST">
        <textarea id="prompt" name="prompt" placeholder="Ingrese su prompt..." required></textarea>
        <button class="btn btn-danger myBtn" type="submit">Generar Imagen</button>
      </form>
      
      <div class="text-center">
        <% if (imageUrl) { %>
          <h1 style="text-align: center">Imagen Generada:</h1>
          <img class="imgGenerate" src="<%= imageUrl %>" alt="<%= creator %>">
        <% } %>
      </div>
  
    </section>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
